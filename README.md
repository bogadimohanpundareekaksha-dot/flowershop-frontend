# Flower Shop Billing Frontend

Static frontend build for the Flower Shop Billing app.

Deploy the repository root as a static site. The app currently calls the backend API URL baked into the built JavaScript bundle.
